name = "plot-money-web"
compatibility_date = "2026-04-30"
compatibility_flags = ["nodejs_compat"]
main = ".svelte-kit/cloudflare/_worker.js"
assets = { directory = ".svelte-kit/cloudflare", binding = "ASSETS" }

[observability]
enabled = true

# ---------------------------------------------------------------------------
# Production: catch-all on plot.money. The api Worker takes more-specific
# /api/*, /mcp, /health routes; everything else (landing, /app dashboard,
# static assets) lands here.
# ---------------------------------------------------------------------------
[env.production]
name = "plot-money-web"
routes = [{ pattern = "plot.money/*", zone_name = "plot.money" }]

# Service binding — direct Worker-to-Worker call into the api Worker.
# SSR load functions use `platform.env.API.fetch(...)` to talk to the
# api without going back out through Cloudflare's edge. Avoids the
# 522 self-loopback that public-fetch hits when both Workers live on
# the same zone.
[[env.production.services]]
binding = "API"
service = "plot-money-api"

[env.production.observability]
enabled = true

# ---------------------------------------------------------------------------
# Preview: served at p.plot.money. Talks to the matching preview API on
# the same hostname (relative URLs — same origin).
# ---------------------------------------------------------------------------
[env.preview]
name = "plot-money-web-preview"
routes = [{ pattern = "p.plot.money/*", zone_name = "plot.money" }]

[[env.preview.services]]
binding = "API"
service = "plot-money-api-preview"

[env.preview.observability]
enabled = true
